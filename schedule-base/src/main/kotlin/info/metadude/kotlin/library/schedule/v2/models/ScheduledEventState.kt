package info.metadude.kotlin.library.schedule.v2.models

enum class ScheduledEventState {
    ACCEPTED,
    CANCELED,
    CONFIRMED,
    DELAYED,
    DELETED,
    DRAFT,
    REJECTED,
    SUBMITTED,
    WITHDRAWN,
    UNKNOWN,
}
